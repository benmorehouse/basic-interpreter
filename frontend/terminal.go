package main

import(
//	"os/exec"
)

//we should take each line in the terminal output, parse into a file, then push to c++ program
func (a *App) ParseBasic(input string){
	// make the text file that will then be fed into the c++ program
}

func (a *App) Interprete(file string){
	// run the 
}

